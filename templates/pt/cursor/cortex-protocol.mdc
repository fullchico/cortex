---
description: Cortex protocol — always consult the vault before writing code, making decisions, or integrating systems. Apply when working on any project that has a Cortex vault.
globs:
alwaysApply: true
---

# Cortex Protocol

## Detectar vault (passo 0 — obrigatorio)

Ler `.cortex` na raiz do projeto:
- Se existe: `vault = JSON.parse(.cortex).vault` (ex: `{"vault":"meu-app"}` → vault = `meu-app`)
- Se nao existe: `vault = cortex` (fallback)
- Se pasta `./<vault>/` nao existe: sugerir `npx cortex-ai`.

Todos os caminhos abaixo usam `./<vault>/` com o valor detectado.

## Antes de codar (OBRIGATORIO)

1. Ler `./<vault>/Dominio/Entidades.md` — campos reais. NAO inventar.
2. Ler `./<vault>/Arquitetura/Padroes de Codigo.md` — copiar padroes. NAO improvisar.
3. Ler `./<vault>/Decisoes/Anti-patterns.md` — o que NUNCA fazer.
4. Ler `./<vault>/Arquitetura/Mapa de Modulos.md` — ja existe? NAO duplicar.
5. Ler `./<vault>/Arquitetura/Estrategia de Testes.md` — testes obrigatorios.

## DDD — ao criar qualquer classe, type ou componente

1. Identificar tipo: Entity (tem ID) / Value Object (imutavel) / Aggregate (raiz de consistencia) / Domain Service (logica sem estado)
2. Verificar bounded context em `./<vault>/Arquitetura/Bounded Contexts.md` — nao cruzar fronteiras
3. Checar `./<vault>/Dominio/Entidades.md` — ja existe? Nao duplicar.
4. Frontend: cada feature module = bounded context. Types devem espelhar o dominio do back.

## SOLID — ao escrever codigo

- **S**: uma classe/componente = uma razao para mudar
- **O**: estender via composicao, nao modificando codigo existente
- **L**: subtipos intercambiaveis com o tipo base
- **I**: interfaces especificas, nao uma interface gorda
- **D**: depender de abstracoes — injetar implementacoes via DI/props/context

## Antes de decidir

6. Ler `./<vault>/Decisoes/Definicoes Travadas.md` — NAO rediscutir.
7. Ler `./<vault>/Decisoes/Questoes em Aberto.md` — nao decidido? PERGUNTAR.
8. Ler `./<vault>/Regras de Negocio/Regras Gerais.md` — formulas reais. NAO adivinhar.

## Antes de integrar

9. Ler `./<vault>/Arquitetura/Contratos API.md` — shape real.
10. Ler `./<vault>/Dominio/Glossario de Dominio.md` — termos corretos.

## Regras

- Vault = fonte da verdade (vault > memoria > codigo)
- NAO adivinhar — perguntar se nao esta no vault
- NAO rediscutir — respeitar Definicoes Travadas
- NAO duplicar — reutilizar o que existe

## Pos-task

Sugerir registrar no vault:
- Termo novo → `./<vault>/Dominio/Glossario de Dominio.md`
- Campo/entidade → `./<vault>/Dominio/Entidades.md`
- Padrao → `./<vault>/Arquitetura/Padroes de Codigo.md`
- Anti-pattern → `./<vault>/Decisoes/Anti-patterns.md`
- Decisao → `./<vault>/Decisoes/Definicoes Travadas.md`
- Duvida → `./<vault>/Decisoes/Questoes em Aberto.md`
